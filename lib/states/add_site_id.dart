import 'package:flutter/material.dart';

class AddSiteId extends StatelessWidget {
  const AddSiteId({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('data');
  }
}
